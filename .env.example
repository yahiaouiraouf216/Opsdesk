# Copiez ce fichier en ".env" et adaptez les valeurs à votre environnement.
# Ne jamais committer de vrais secrets.

DATABASE_URL=postgresql://username:password@localhost:5432/opsdesk
SECRET_KEY=change-me
APP_ENV=development
PORT=5000

# Utilisée uniquement par la suite de tests pytest (base séparée)
TEST_DATABASE_URL=postgresql://username:password@localhost:5432/opsdesk_test
